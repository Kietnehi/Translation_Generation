# Frontend (Vite + React)

From repo root, run:

```bash
cd frontend
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` and proxies `/translate` to `http://localhost:8000` (the FastAPI backend).
